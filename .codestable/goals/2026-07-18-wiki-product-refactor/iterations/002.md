---
doc_type: goal-iteration
goal: wiki-product-refactor
iteration: 2
status_after: active
next_action: "继续 S4：用显式 ViewModel store 覆盖首页、列表、详情、搜索与无包启动的 Route 路径。"
blocker_signature: null
functional_acceptance: null
updated_at: 2026-07-18
---

# Iteration 002

## 当前理解

安全网需要在真实导入链之上覆盖查询和个人数据连续性，不能用 mock 替代。

## 本次实现

- 新增类型、列表、稳定详情 ID、别名、中文/英文/拼音与 FTS 查询测试。
- 新增普通 `entity_search` 表导致 FTS 查询失败的可观察测试。
- 新增 A→B 切换后收藏、历史、笔记、搜索历史保留与缺失记录清理测试。
- 为首页分类入口加入不可见 `home-category:<type>` selector。

## 验证证据

- `./gradlew.bat :app:compileDebugAndroidTestKotlin` 通过。
- `git diff --check` 通过。

## 遇到的问题

新增测试曾缺少一个结果解包 import，已在本次内修复并重新编译通过。设备仍不可用，故真实运行结果待统一执行。

## 下一次尝试

继续实现 S4 的 Route 级 Compose 导航测试；所有 ViewModel 继续通过测试场景的唯一 store 和显式 factory 创建。
