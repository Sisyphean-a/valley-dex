---
doc_type: goal-iteration
goal: "stardew-offline-android"
iteration: 8
status_after: active
next_action: "执行最终 Debug/Release 构建、真实数据包与设备可用性验收。"
blocker_signature: null
updated_at: "2026-07-18"
---

# Iteration 008

## 当前理解

性能基线和仪器测试必须是实际可编译的 Android 测试，而不能只写文档。执行性能测试需要真实设备或已启动模拟器。

## 实现方案

加入 Compose UI 测试、真实内置数据包校验测试、Baseline Profile 场景和 Macrobenchmark 冷启动/帧时间场景；构建 Benchmark APK 验证 API 与依赖配置。

## 本次变更

- 增加 Compose `EntityListItem` 仪器测试。
- 增加启动、搜索路径的 Baseline Profile 生成器与 Macrobenchmark。
- 将 Baseline Profile 模块 Java/Kotlin 目标统一为 17。

## 验证证据

`./gradlew.bat :app:compileDebugAndroidTestKotlin :baselineprofile:assembleBenchmarkRelease` 成功。此前完整 Benchmark 装配在连接测试阶段报告 `No connected devices`。

## 遇到的问题

本机 Android SDK 没有系统镜像，`.android/avd` 也没有已创建的设备；因此无法执行连接测试或生成真实性能数字。

## 下一次尝试

运行最终单元测试、Lint、Debug/Release 构建，检查真实 `.svdata` 和 APK 产物；有设备则立即执行连接测试。

## 状态更新

目标保持 `active`，设备缺失为环境限制，不改变已编译测试的状态。
