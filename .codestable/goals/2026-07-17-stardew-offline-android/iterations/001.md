---
doc_type: goal-iteration
goal: "stardew-offline-android"
iteration: 1
status_after: active
next_action: "实现真实数据包安装、校验、激活与只读内容数据库启动链路。"
blocker_signature: null
updated_at: "2026-07-17"
---

# 迭代 001

## 当前理解

项目从空工程开始，数据包已核对为 schema 2，真实库包含 3688 个实体。第一阶段先建立能持续编译的 Android 基线，并先落盘界面设计。

## 实现方案

采用单模块 Android 应用加独立 Baseline Profile 模块。使用 Kotlin、Compose、Hilt、Room、DataStore 和版本目录；不声明网络权限。真实数据保持在被忽略的 `database` 目录，后续作为本地可选资源安装。

## 本次变更

- 创建 Gradle Wrapper、Kotlin DSL、版本目录、应用和 Baseline Profile 模块。
- 建立 Hilt 应用入口、单 Activity、Material 3 静态主题、备份排除规则和基础 `EntityId`。
- 写入信息架构与视觉设计，完成无网络的应用清单。
- 修复启动图标和备份 XML 的资源/Lint 错误。

## 验证证据

- `assembleDebug` 成功，生成 `app/build/outputs/apk/debug/app-debug.apk`。
- `testDebugUnitTest` 成功，`EntityIdTest` 通过。
- `lintDebug` 成功；报告已生成。

## 遇到的问题

首次构建缺少启动图标，已补充；备份配置中使用了无效的 `cache` 域，已移除。无阻塞问题。

## 下一次尝试

实现数据包解压、manifest/哈希/SQLite 校验、DataStore 激活、自动内置包安装和导入状态页。

## 状态更新

目标保持 active，当前迭代为 1。
