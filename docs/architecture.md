# 架构

界面通过 ViewModel 的不可变状态调用 Repository。内容 Repository 使用只读 `SQLiteDatabase`，用户 Repository 使用独立 Room 数据库；DataStore 保存当前和上一个数据包标识。

数据包安装到 staging，校验成功后按目录切换。切换前关闭内容库；新库打不开时恢复旧标识并重新打开旧库。收藏、历史、笔记是软引用，因此更新内容包不会丢失。

搜索先安全标准化输入，再合并中文、英文、拼音、首字母、别名和 FTS 结果；应用层保留最高评分并按拼音排序。关系与未知 JSON 字段应按存在性展示，不解释旧式数组字段。
